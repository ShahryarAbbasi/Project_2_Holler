<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css" />
    <title>Holler</title>
</head>
<body>
    <nav class="nav">
        <a class="nav-hollers" href="/hollers">Hollers</a>
        <a class="nav-home" href="/">Home</a>
      </nav>
    <h1><%=user.handle%>'s Hollers</h1>
    <div class="timeline"> 
        <% for(let i = (hollers.length - 1); i >= 0; i--) {%>
            <% let userId = hollers[i].user %>
            <%if (userId == user.id) {%>
                <div class="holler">
                    <p><a href="/hollers/<%=hollers[i].id%>"><%= hollers[i].body%></a></p>
                    <img src='<%=hollers[i].image%>'>
                </div>
            <% } %>
        <% } %>
    </div>
</body>
</html>